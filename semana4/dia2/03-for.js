/**
 *
 */

for (let c = 0; c < 10; c++) {
  console.log(`12 x ${c} = ${12 * c}`);
}

/**
 * Crear un algoritmo para multiplicar 2 números
 * sin utilizar el signo de * (multiplicación)
 */

let a = 5;
let b = 3;
let sumatoria = 0;
for (let c = 0; c < b; c++) {
  sumatoria = sumatoria + a;
}
console.log(`${a} x ${b} = ${sumatoria}`);
